import { makeStyles } from "@material-ui/core";

export const useStyle=makeStyles({
    root:{
        marginTop:'6rem'

    },
    btn:{
        outline: 'none',
        borderRadius: '20px',
        padding:'.5rem 2rem',
        marginTop: '1rem '
    },
})